// Mini Project: Score Calculator

const passMark = 50;

// Function to check if the score is a pass
const isPass = (score: number): boolean => {
    // return true if score is 50 or above
};

// Function to format the result message
const getResultMessage = (score: number): string => {
    // use isPass(score)
    // return a formatted string
};

const score = 72;

// Log the result